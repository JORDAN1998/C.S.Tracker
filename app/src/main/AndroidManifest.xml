<?xml version="1.0" encoding="utf-8"?>

<!-- Log Of All Activities, Broadcasts, And Permissions, Used By The App -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jordanzimmittidevelopers.com.communityservicelogger">

    <!-- Allows App To Write To External Storage For Backup, Restore, And Print Features -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Allows App To Read To External Storage For Backup, Restore, And Print Features -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Allows App To Get Users Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Allows App To Vibrate -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Allows App To Add App Shortcut To Launcher -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <!-- Allows App To Take Away App Shortcut From Launcher -->
    <uses-permission android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT" />

    <!-- Allows App To Access The Internet (Google Drive) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />


    <!-- Basic Application Information -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/red"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/RedTheme">

        <!-- Activity DefaultActivity -->
        <activity
            android:name=".DefaultActivity"
            android:label="@string/app_name">

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity DefaultActivityBlue -->
        <activity-alias
            android:name=".DefaultActivityBlue"
            android:enabled="false"
            android:icon="@mipmap/blue"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityGreen -->
        <activity-alias
            android:name=".DefaultActivityGreen"
            android:enabled="false"
            android:icon="@mipmap/green"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityIndigo -->
        <activity-alias
            android:name=".DefaultActivityIndigo"
            android:enabled="false"
            android:icon="@mipmap/indigo"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityOrange -->
        <activity-alias
            android:name=".DefaultActivityOrange"
            android:enabled="false"
            android:icon="@mipmap/orange"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityPink -->
        <activity-alias
            android:name=".DefaultActivityPink"
            android:enabled="false"
            android:icon="@mipmap/pink"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityYellow -->
        <activity-alias
            android:name=".DefaultActivityYellow"
            android:enabled="false"
            android:icon="@mipmap/yellow"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultActivityViolet -->
        <activity-alias
            android:name=".DefaultActivityViolet"
            android:enabled="false"
            android:icon="@mipmap/violet"
            android:label="@string/app_name"
            android:targetActivity=".DefaultActivity" >

            <!-- Tell The App To DefaultActivity Activity At Start -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Activity DefaultUser -->
        <activity
            android:name=".DefaultUser"
            android:label="@string/activity_default_user" />

        <!-- Activity EventsAdd -->
        <activity
            android:name=".EventsAdd"
            android:label="@string/activity_events_add" />

        <!-- Activity EventsEdit -->
        <activity
            android:name=".EventsEdit"
            android:label="@string/activity_events_edit" />

        <!-- Activity EventsExtraInformation -->
        <activity
            android:name=".EventsExtraInformation"
            android:label="@string/activity_events_extra_information" />

        <!-- Activity EventsSync -->
        <activity
            android:name=".EventsSync" />

        <!-- Activity EventsView -->
        <activity
            android:name=".EventsView" />

        <!-- Activity google_drive_sync -->
        <activity
            android:name=".GoogleDriveSync" />

        <!-- Activity RemindersAdd -->
        <activity
            android:name=".RemindersAdd"
            android:label="@string/activity_reminders_add" />

        <!-- Activity RemindersEdit -->
        <activity
            android:name=".RemindersEdit"
            android:label="@string/activity_reminders_edit"/>

        <!-- Activity RemindersView -->
        <activity
            android:name=".RemindersView"
            android:label="@string/activity_reminders_view" />

        <!-- Activity SettingsAbout -->
        <activity
            android:name=".SettingsAbout"
            android:label="@string/activity_settings_about" />

        <!-- Activity Settings -->
        <activity
            android:name=".Settings"
            android:label="@string/activity_settings" />

        <!-- Activity SettingsThemePicker -->
        <activity
            android:name=".SettingsThemePicker"
            android:label="@string/activity_theme_picker" />

        <!-- Activity UsersAdd -->
        <activity
            android:name=".UsersAdd"
            android:label="@string/activity_users_add" />

        <!-- Activity UsersEdit -->
        <activity
            android:name=".UsersEdit"
            android:label="@string/activity_users_edit" />

        <!-- Activity UsersView -->
        <activity
            android:name=".UsersView"
            android:label="@string/activity_users_view" />

    </application>

</manifest>